// Create Event

ground = true;    // If we are on the ground
//isStep = false;   // Are we sidestepping
//stepTimer = 0;    // Timer for sidestepping
//initStep = true;  // If we are going to move, do we need to step first
//vel = 0;          // Current movement speed

// Physics Variables
//jumpSpeed = 4.75;     // Speed of jump
//walkSpeed = 1.296875; // Speed of walking
//moveSpeed = walkSpeed; // Speed of air movement (added this line)
//grav = 0.25;          // Gravity
//maxVspeed = 7;        // Maximum vertical speed
//stepSpeed = 1 / 7;    // Speed for sidestepping
//stepFrames = 7;       // Frames for a sidestep

xsp=0
ysp=0

isShoot=false;//are we currently shooting

//Sprites
Idle = SpriteIdle;
Step = SpriteStep;
Walk = SpriteWalk;
Jump = SpriteJump;
Shoot = SpriteShoot;